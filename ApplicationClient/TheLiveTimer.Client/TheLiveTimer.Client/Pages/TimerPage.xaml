<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="TheLiveTimer.Client.TimerPage">
	<ContentPage.Content>
		<StackLayout>
			<Label Text="Welcome to the Timer page!" VerticalOptions="Center" HorizontalOptions="Center" />


        <Grid BackgroundColor="{Binding Path=VisualSettings.BackgroundColor}">

            <!-- Message -->
            <Label Text="{Binding Message}"
                TextColor="{Binding Path=TimerColor}" 
                FontSize="{Binding Path=MessageFontSize}" 
                FontFamily="{Binding Path=VisualSettings.TimerFontFamily}" 
                HorizontalTextAlignment="Center" 
                VerticalTextAlignment="Center"
                       IsVisible="False">

            </Label>

            <!-- Timer -->
            <Label Text="{Binding Timer}" 
                   TextColor="{Binding Path=TimerColor}" 
                   FontSize="{Binding Path=VisualSettings.TimerFontSize}" 
                   FontFamily="{Binding Path=VisualSettings.TimerFontFamily}" 
                   HorizontalTextAlignment="Center" 
                   VerticalTextAlignment="Center">

            </Label>

        </Grid>

        <Label Text="This is my text. fgfg Yeah" />
            
		</StackLayout>
	</ContentPage.Content>
</ContentPage>